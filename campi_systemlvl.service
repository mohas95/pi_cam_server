[Unit]
Description = Campi Server using Flask and Gunicorn
# After=network.target
After=network-online.target
Wants=network-online.target

[Service]
User=pi
WorkingDirectory=/home/pi/pi_cam_server

ExecStartPre=/bin/sleep 5

ExecStart=/usr/bin/gunicorn --workers 1 --threads 4 --timeout 120 --bind 0.0.0.0:5000 --capture-output --log-level debug app:app
Restart=always

[Install]
WantedBy=multi-user.target
